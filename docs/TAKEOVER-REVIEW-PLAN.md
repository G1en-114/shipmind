# ShipMind 接手审查与后续计划

审查日期：2026-09-22。基线提交：`0f03ca4`。本轮任务：审查、确定剩余工作、写计划后停止。下列修复均尚未实施。

后续工作骨架已建立：[执行入口与任务板](work/README.md)。包含任务依赖、验收规格、制品清单、执行记录模板和提交物料槽位；截至搭架结束，所有实施任务仍为 pending。

## 结论

项目已经具备可运行的本地演示原型：声学、视觉、航线、雷达、声纹、检索、报告和 Web 均有实现。当前主要缺口不是继续增加 Skill，而是让干净环境可复现、失败时不产生误导结论、展示与实际能力一致，并完成提交物料。

不能沿用交接报告“31/31、全部可验证”作为当前验收结论。现有回归会漏掉自检失败、路由执行失败、报告遗漏和错误证据引用；本地缓存的夹具与模型也掩盖了首次部署缺口。

## 本轮审查范围与验证记录

- 阅读 README、交接报告、项目方案、总控清单、部署文档、评测说明，审查自研 Harness、评测运行器、演示链路、Web、部署脚本及声纹训练划分代码。未发现仓库内 AGENTS.md。
- 初始 Git 工作区干净。本轮只新增本文；验证产生的输出在忽略目录 `runs/` 和既有演示夹具目录。未修改业务代码、未提交或推送。
- 使用现有评测的参数构建与断言运行 27 条不需要大脑/语音端点的用例：首次 26/27，融合例失败。首次显式设置子进程输出 UTF-8，但内部读取仍沿用 Windows 默认编码，暴露嵌套进程编码不一致。为进程树统一设置 `PYTHONUTF8=1` 后单独重跑融合例通过。因此是“27 条分别通过，存在编码前提”，不是一次完整 31/31。
- 本次未运行大脑路由 3 例和在线 TTS 1 例，未调用 StepFun、未连接或部署 GB10。历史真机、训练及在线端点结果未重新认证。
- `python -m harness.selftest` 实测退出 1：声学路径缺少 `anom/`。
- FastAPI TestClient 六接口在统一 UTF-8 环境下均返回 200；未进行本轮浏览器视觉验收。编码混用时部分接口即使子进程解码失败仍返回 200，说明只检查 HTTP 状态不足。
- 最小反例：证据 `range_m=1`、结论“range 9999 m”，verifier 仍通过；大脑输出非 JSON 时，相关声学请求返回 `(None, 'brain')`，不走关键词兜底。
- 用临时小目录验证 `tarfile.add` 默认递归行为：添加父目录会包含被外层筛选排除的子目录。未打包真实数据或上传文件。
- 本地评测明细：`runs/takeover-audit/offline-evals.json`，记录首次 26/27，不应误作统一 UTF-8 重跑结果。视觉本轮返回有效读数 n=72、拒判 4、归一化 MAE=0.0558，不能称为全部 130 张的独立验证。

## 需要解决的问题

P0 指会阻断提交演示可信度或首次部署；P1 指冻结前应处理；P2 为可延期增强。

| 编号 | 优先级 | 发现与证据 | 后续处理与验收 |
|---|---|---|---|
| R01 | P0 | `requirements.txt` 只有 numpy；视觉/Web/训练/运维实际还用 Pillow、FastAPI、uvicorn、scipy、paramiko。`make_fixtures.py` 不生成表盘与报告正负例；声纹 ML 权重被 Git 忽略。运行器仅检查 fixtures 根目录存在。 | 区分核心、Web、训练、运维依赖；补齐夹具与模型来源清单、校验和和缺失提示。在没有本地缓存的新目录按文档完成离线基线和 Web 启动。 |
| R02 | P0 | `scripts/node_deploy.py:21` 排除整个 models，连训练代码也漏；`:42` 对目录默认递归添加，会绕过嵌套排除并重复归档文件。 | 改成明确文件清单或受控遍历，逐文件归档；模型单列受控制品。验证压缩包不含凭据、运行输出、夹具/数据大包，不重复成员，包含必要代码和语料；验证远端退出状态。修复前不按旧交接建议直接“整仓部署”。 |
| R03 | P0 | `fusion_demo.py:64` 在 targets=[] 时索引越界；失败时又可能用默认 45° 方位继续推导。子进程返回码未统一检查，`:164` 不依据审核结果决定退出状态。 | 无目标、传感器失败、空输出、超时分别形成显式状态；缺证据停止相应结论和播报。给融合增加这些失败场景，禁止用默认观测补齐缺失数据。 |
| R04 | P0 | verifier 只检查引用存在且非空；`compose_report.py` 却标成“已核实”。融合另生成一份 claims，而最终报告重新提炼，遗漏会遇结论与手册引用；本轮实测 report_md 无“会遇态势”。 | 统一唯一的结论/证据对象供审核、报告、UI、播报使用；结构检查应标为“引用已校验”。数值、单位、对象 ID 采用结构化逐项比对，矛盾引用必须拒绝；最终输出逐条与通过项一致。无需扩展成通用语义判官。 |
| R05 | P1 | `harness/selftest.py` 声学夹具路径过期，导致 smoke 必败；内部 subprocess 普遍未固定编码或超时。 | 修自检路径，统一进程编码、超时、异常及返回码处理。Windows 与 GB10 各通过一次完整冒烟；卡住的 Skill 必须在限时内返回可追踪失败。 |
| R06 | P1 | `brain.py:106–114` 将解析错误与合法 NONE 混同；`orchestrator.py:49` 因此跳过兜底。`:47` 候选只保留有 entry 的 Skill，5 个官方 advisory 不进入正常大脑候选；`.env` 覆盖进程环境，切端点可能失效。 | 区分成功路由、明确拒绝、解析失败、端点失败；失败可追踪地降级。定义 advisory 的路由策略，环境变量优先。用替身端点测失败路径，再分别实测本地/在线路由；不得把关键词命中算作模型命中。 |
| R07 | P1 | `agent_demo.py` 仅给 5 个自研模块提供固定参数，其他可路由模块会“无演示参数”；执行失败仍返回 0。文档描述的 verifier/播报并未在此实现，`--no-voice` 未使用。 | 明确它是路由演示还是完整工作流入口；本轮后建议接入一条机舱闭环，展示真实输入绑定、Skill 结果和审核轨迹。所有公开入口有可执行参数或明确拒绝；失败退出非零。 |
| R08 | P1 | `run_evals.py` 路由测试不要求执行成功；多个断言不限制成功返回码；无识别断言默认 pass；官方输出 None 可触发异常。视觉只抽 train 最多 60 + val 最多 20，漏检样本直接跳过。 | 分离离线/在线/真机测试，明确 skip 与失败；未知断言应失败；增加执行成功、结果内容、报告完整性、定位漏检率、读数覆盖率和最大误差检查。保存版本、环境、耗时、制品指纹，不只保存总通过数。 |
| R09 | P1 | `web/server.py` 告警时间固定，valve 实为 pump 录音和 pump 基线；轨迹 latest 按路径名排序；多个 API 把失败转成空结果，前端多处空 catch。 | 明示合成场景、真实设备来源及采集/生成时间；按运行 ID 绑定六面板，正确选择最近轨迹；显示错误/过期状态。验收两次不同场景切换及一次失败，不仅六个 200。 |
| R10 | P1 | `models/train_sonar.py` 按文件名划分，无船舶身份分组证据；不能据此保证“按船防泄漏”。视觉历史极端反向错误尚未重新定位。声学 AUC 和 0.431 声纹准确率是历史记录。 | 保存录音/船舶分组与划分清单；没有船 ID 就限定为 recording-wise 并说明跨录音同船风险。优先复现视觉离群并增加拒判，不先追求提升平均分；冻结结果须附数据清单、脚本版本和指标口径。 |
| R11 | P1 | README 的 13/13、报告/Web 待完成、NIM 本地 Step 部署与 HANDOFF 不一致；“运行时零云端”与在线语音冲突。官方执行实际为管线生成脚本，未证明生成的 GStreamer 管线已运行；其他 5 个仅 advisory。 | 统一能力矩阵：已实现/已实测/仅建议/未实现；分清离线核心、在线增强、预录音。断网演示真实禁网重跑，不能用预录播报证明实时离线 TTS。统一 README、方案、清单与架构图。 |

本轮不提供法律、资费或赛事规则的重新核验结论。提交前需根据官方原始来源另行核验截止时间、提交要求、法条推论、费用与数据许可；仓库中“已核实”字样不代替本轮证据。特别是从通信转接要求推导“数据必须不出船”的论证需单独审定。

## 执行顺序与时间预算

以下沿用仓库记载的 9 月 29 日提交窗口作工作假设；时间为估算，未开始实施。

| 阶段 | 建议时间 | 任务 | 完成门槛 |
|---|---|---|---|
| 1. 固定可复现基线 | 9/23，约 0.5–1 天 | R01、R02、R05；补依赖、夹具、制品清单、自检、编码和打包 | 干净目录能运行离线测试；部署包内容审核通过；不依赖旧 runs/fixtures |
| 2. 修正核心失败语义 | 9/24，约 1 天 | R03、R04、R06、R07，先保住机舱主线与一个态势场景 | 成功、无目标、缺输入、错误引用、路由失败五类场景均有正确退出和显示；报告与审核同源 |
| 3. 测试与展示闭环 | 9/25，约 1 天 | R08、R09；复现视觉离群，整理 R10 指标边界 | 内容级回归通过；两个场景和故障场景可录屏；冻结新增功能 |
| 4. 真机复核与材料口径 | 9/26，约 0.5–1 天 | 在部署包审查通过后同步官方 GB10；跑离线基线、本地大脑、Web 和端到端；另列在线语音测试；完成 R11 | 同一提交的设备、环境、耗时、日志和制品清单齐全；离线演示无外网请求；未通过项显式列出 |
| 5. 提交物料 | 9/27–28 | 6 分钟脚本、录屏与剪辑、征文整理、合影、README 定稿 | 仓库、视频、征文、合影四项准备就绪；发布/提交由后续明确授权执行 |
| 6. 提交缓冲 | 9/29 | 复查可访问链接和最终版本，按已核实规则提交 | 记录提交回执；仅修阻断问题 |

实施时阶段 1→2→3 为依赖顺序，材料脚本可在功能边界确定后穿插准备。若超时，优先保证机舱闭环、可信的离线演示及提交物；压缩态势展示和可选功能。无需为了“多智能体”叙事临时增加协商系统。

## 延期与待补信息

- P2：WebSocket/连续告警流、复杂轨迹 diff UI、TAO/VSS/RAG Blueprint 实际服务升级、更大本地模型、ASR 深度优化、AE 重训练和声纹冲榜。只有关键阶段提前完成才考虑，不能抢占录屏时间。
- 语音最低要求：在线 TTS/ASR 状态和失败降级可验证；离线版先文本提示，预录音需清晰标注。真正离线 ASR/TTS 是单独功能，不在本计划中暗示已完成。
- 待用户在实施/发布阶段补充：B 站账号与发布安排、征文平台、团队公开署名及合影、是否坚持实时离线语音。本轮写计划不依赖这些答案。
- 延续交接边界：只用本地与官方 GB10；不启用付费服务器、不做安全方向、不用军用声纹叙事；手册保持自拟并明示合成来源。

## 本轮停止点

审查与计划已完成。未启动上述修复、部署、训练、在线调用或材料发布。后续从阶段 1 开始，不直接沿用旧交接报告“先同步节点”的顺序。
